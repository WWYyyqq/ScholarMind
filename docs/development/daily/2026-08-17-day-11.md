# Day 11：语义证据验证、可重复评测与 Silver 复核工具

日期：2026-08-17

状态：🟡 代码与离线测试完成；真实 Qwen 语义评测等待 WSL GPU 恢复

分支：`codex/semantic-verifier-eval`

## 1. 承接状态与目标

本日从已合并的 Day 10 `main@6250eb6` 开始，先复核代码、模型目录、项目虚拟
环境和 GitHub 状态，不重复下载模型或重建 10,879 条论文向量。

Day 10 的 File Researcher 已能独立完成检索、抽取式 Claim、页码 Citation 和
HTTP API，但确定性 Verifier 只能可靠处理逐字匹配、数字和否定极性。今日目标是：

1. 在不降低原有发布门的前提下增加 Claim 级语义判断；
2. 将 Verifier 接入 Service、CLI、LangGraph API 和 `doctor`；
3. 建立不含私有论文的可重复分类评测；
4. 把 42 道 Silver 的冻结和分批工作从手工命令变成安全工具；
5. 运行单元测试、真实模型测试和 CI；任何未实际完成的验收明确记录。

## 2. 语义 Verifier 设计

### 2.1 两层发布门

新链路不是让大模型替代规则，而是组合两层判断：

```text
Claim + linked Evidence
        │
        ▼
确定性硬门：Evidence ID / 数字 / 极性
        │ hard fail ───────────────► 拒绝发布
        │ pass
        ▼
严格抽取式匹配 ── supported ─────► 直接发布，不调用模型
        │ lexical uncertainty
        ▼
Qwen JSON Schema 语义判断
        │ supported + confidence >= threshold + valid evidence indices
        ├──────────────────────────► 发布并生成 Citation
        └─ 其他 / 超时 / 非法输出 ─► 拒绝发布
```

数字矛盾、否定冲突、空 Evidence 或不存在的 Evidence ID 会设置
`hard_gate_passed=false`。语义模型没有覆盖权，避免模型用流畅解释绕过可确定的
事实错误。

### 2.2 结构化模型契约

本地 Qwen3-14B 通过 OpenAI-compatible `/v1/chat/completions` 返回：

- `label`：`supported|partial|contradicted|insufficient`；
- `confidence`：0～1；
- `reason`：简短判定理由；
- `supporting_indices`：1-based Evidence 下标。

请求使用 JSON Schema 限制字段和类型，代码再次执行严格解析：拒绝额外字段、
越界下标、布尔型伪数值，以及没有证据索引的 `supported`。Evidence 被明确标成
不可信数据，不能作为指令执行。

### 2.3 Fail-closed 与资源释放

- 语义置信度低于默认 0.75：降为 `partial`，不得发布；
- 模型超时、连接失败、空响应、JSON 错误：保留确定性结果并标记
  `semantic-fallback`，不得伪装成 supported；
- Service 正常关闭和组装异常均释放 Verifier HTTP Client；
- 默认 `verification_mode=deterministic`，无 GPU 的 CI 和开发环境仍可运行。

## 3. Service、CLI、API 与自检接入

新增项目变量：

```dotenv
SCHOLARMIND_VERIFICATION_MODE=semantic
SCHOLARMIND_VERIFIER_MODEL=qwen3-14b-local
SCHOLARMIND_VERIFIER_BASE_URL=http://[::1]:8000/v1
SCHOLARMIND_VERIFIER_API_KEY=local-not-required
SCHOLARMIND_VERIFIER_MINIMUM_CONFIDENCE=0.75
```

CLI：

```bash
.venv/bin/python -m scholarmind.cli research \
  "研究问题" \
  --retrieval-mode hybrid-rerank \
  --verification-mode semantic
```

LangGraph 输入增加 `verification_mode`，输出同时回显该模式。每条 Verification
增加 `confidence`、`verification_method` 和 `hard_gate_passed`，便于面试演示时
解释“哪个规则或模型作出了决定”。

`scholarmind doctor --verification-mode semantic` 不只检查端口健康；它要求模型对
安全合成的同义句返回结构化 `supported` 和有效 Evidence 索引，失败信息只包含
错误类型，不回显密钥或论文内容。

本地 LLM 启动脚本现支持 `SCHOLARMIND_VLLM_ENV`，因此不同 Git worktree 可以复用
同一个 ScholarMind 项目专用 vLLM 环境，而不复制约 7.6 GB 依赖或污染系统 Python。

## 4. 可重复 Verifier 评测

新增 8 条公开合成案例，覆盖：

- 严格抽取支持；
- 同义改写支持；
- 附加限定导致部分支持；
- 数字矛盾；
- 否定极性矛盾；
- `A outperforms B` / `B outperforms A` 关系反转；
- 无关证据；
- 两段 Evidence 联合支持。

确定性基线真实执行结果：

| 指标 | 结果 |
| --- | ---: |
| 案例数 | 8 |
| 分类准确率 | 62.50% |
| 发布精确率 | 100.00% |
| 发布召回率 | 33.33% |

它正确体现“安全但保守”：没有发布错误的支持项，但漏掉同义改写、多证据组合，
并把关系反转判为 partial。语义模式必须在同一文件上提升这些案例，同时保持数字
与极性硬门，才算有效增益。

运行命令：

```bash
.venv/bin/python scripts/evaluate_claim_verifier.py --mode deterministic
.venv/bin/python scripts/evaluate_claim_verifier.py --mode semantic
```

输出只含案例 ID、标签、置信度、方法和聚合指标，不包含私有论文。

## 5. Silver 人工复核工具

此前公开文档已经规定双轮协议，但冻结哈希、创建 42 个 ID 和分批仍需手工完成。
新增 `scripts/prepare_silver_review.py`：

1. 用正式 Silver Schema 校验每条记录；
2. 严格要求本次固定版本为 42 题（19 摘要、23 方法定位）；
3. 对配置、清单、test corpus、Silver 和全部解析文档计算 SHA-256；
4. 创建 5/12/13/12 四个批次；
5. 生成两轮顺序不同的 ID-only 队列；
6. 生成 42 份已绑定来源 ID 和输入哈希的空 Review 记录；
7. 目标目录存在时拒绝覆盖，保护已经填写的人工成果。

真实题目、答案、证据和论文仍留在原始私有数据集，GitHub 只提交工具、空模板和
流程。当前机器未找到原 `questions.silver.jsonl`，因此本日没有猜测或公开 4 条
争议题 ID，也没有伪造“已生成真实队列”的结论。找到原 `<DATASET_OUTPUT>` 后运行：

```bash
.venv/bin/python scripts/prepare_silver_review.py \
  --dataset "<DATASET_OUTPUT>" \
  --output "<ANNOTATION_ROOT>/paper-eval-v1"
```

## 6. 测试与当前真实运行状态

### 6.1 已通过

- Ruff：新增生产代码、脚本和测试通过；
- 语义 Verifier、设置、Service、Graph、Health 和 Silver batching 单元测试；
- 完整单元测试按同步与异步执行器分区复验：141 passed；
- 确定性 8 案例评测真实运行完成。

本地 Docker Desktop 中 PostgreSQL 容器仍为 healthy，但当前 WSL Integration
未启用，Ubuntu 无法连接 Windows loopback 的 5433 端口；本地 pgvector 测试因此
记录为运行时不可达，而不是测试通过。PR 的 GitHub Actions 会启动独立
`pgvector/pgvector:pg16` service。PR #12 的 CI Run `32023892801` 已在 39 秒内
通过锁文件、Ruff、完整单元测试和真实 pgvector 集成测试，满足合并门。

### 6.2 真实 Qwen 暂未通过的原因

模型权重和 vLLM 项目环境均存在，但当前 Ubuntu 会话没有 `/dev/dxg`：

- Windows `nvidia-smi.exe`：RTX 3090 Ti 正常，24,564 MiB 显存，仅占约 180 MiB；
- WSL `nvidia-smi`：`GPU access blocked by the operating system`；
- vLLM：`Failed to infer device type`。

这不是模型下载、路径、Python 环境或 Verifier 代码错误。恢复通常需要
`wsl --shutdown`，但该命令会结束 Codex 和所有 WSL 进程，所以本日没有在工作中
擅自执行。重开 WSL 后必须补跑：

```bash
SCHOLARMIND_VLLM_ENV=/path/to/ScholarMind/services/local-llm/.venv \
  ./services/local-llm/start.sh
services/local-llm/.venv/bin/python services/local-llm/smoke_test.py
.venv/bin/python scripts/evaluate_claim_verifier.py --mode semantic
```

未得到真实输出前，简历只能写“实现可选本地 Qwen 语义验证与评测框架”，不能写
“语义评测达到某个准确率”。

## 7. 本日文件

主要新增：

- `src/scholarmind/verification/semantic_verifier.py`；
- `scripts/evaluate_claim_verifier.py`；
- `evaluation/claim-verifier/cases.synthetic.jsonl`；
- `scripts/prepare_silver_review.py`；
- `tests/unit/test_semantic_verifier.py`；
- `tests/unit/test_prepare_silver_review.py`。

主要修改：Settings、Service、File Researcher、Graph、CLI、Health、环境示例、本地
启动脚本、主页、架构/运行/评测/标注文档。

## 8. 下一步

1. 重启 WSL 后完成真实 Qwen 8 案例语义评测；
2. 找回原 42 题私有构建目录，生成冻结复核工作区并定位 4 条争议题；
3. 实现正式 Review/Gold JSON Schema、合并器、证据 bbox 重算和 Gold 构建器；
4. 在真实 Gold 上重跑 Dense / Hybrid / Rerank 与 Verifier 消融；
5. 再把语义验证接入上游 Web Writer，而不是让两个 Agent 共享不清晰的隐式状态。
